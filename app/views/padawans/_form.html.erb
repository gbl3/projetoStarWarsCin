<%= form_with(model: padawan, local: true) do |form| %>
  <% if padawan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(padawan.errors.count, "error") %> prohibited this padawan from being saved:</h2>

      <ul>
      <% padawan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cod_padawan %>
    <%= form.text_field :cod_padawan, id: :padawan_cod_padawan %>
  </div>

  <div class="field">
    <%= form.label :id_usuario_j %>
    <%= form.text_field :id_usuario_j, id: :padawan_id_usuario_j %>
  </div>

  <div class="field">
    <%= form.label :nome_pad %>
    <%= form.text_field :nome_pad, id: :padawan_nome_pad %>
  </div>

  <div class="field">
    <%= form.label :idade %>
    <%= form.text_field :idade, id: :padawan_idade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
