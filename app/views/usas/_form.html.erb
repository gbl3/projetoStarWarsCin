<%= form_with(model: usa, local: true) do |form| %>
  <% if usa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usa.errors.count, "error") %> prohibited this usa from being saved:</h2>

      <ul>
      <% usa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_usuario %>
    <%= form.text_field :id_usuario, id: :usa_id_usuario %>
  </div>

  <div class="field">
    <%= form.label :id_fabricacao %>
    <%= form.text_field :id_fabricacao, id: :usa_id_fabricacao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
